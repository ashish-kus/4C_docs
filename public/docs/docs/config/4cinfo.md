# 4C.info

The 4C.info file resides in the HOME_4C directory.

It is used by the 4C install process to know where
to install files and how to set the permissions
on installed files.

It is also used by 4c server processes
to set the default values for some
env vars if _4CSRVRCONFIG does not set
them.

