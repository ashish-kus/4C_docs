<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>4C Language Docs – 4C Version Control</title><link>https://127.0.0.1:8080/docs/features/versioncontrol/</link><description>Recent content in 4C Version Control on 4C Language Docs</description><generator>Hugo -- gohugo.io</generator><language>en</language><atom:link href="https://127.0.0.1:8080/docs/features/versioncontrol/index.xml" rel="self" type="application/rss+xml"/><item><title>4C Version Control Change Log</title><link>https://127.0.0.1:8080/docs/features/versioncontrol/changes/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://127.0.0.1:8080/docs/features/versioncontrol/changes/</guid><description>
&lt;h2&gt;28 February 2006&lt;span class="hx:absolute hx:-mt-20" id="28-february-2006"&gt;&lt;/span&gt;
&lt;a href="#28-february-2006" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;ol&gt;
&lt;li&gt;The source code control bootstrap programs will no longer allow you to reedit an installed program, file, or layout on the same mod. Reedit can still be used to reedit a completely unedited program, file, or layout.&lt;/li&gt;
&lt;li&gt;The source code control bootstrap programs will no longer allow you to delete the sc_prog, sc_file, or sc_slhdr rcd for an installed program, file, or layout. You can still delete the rcd corresponding to a completely unedit program, file, or layout.&lt;/li&gt;
&lt;/ol&gt;
&lt;h2&gt;09 October 2005&lt;span class="hx:absolute hx:-mt-20" id="09-october-2005"&gt;&lt;/span&gt;
&lt;a href="#09-october-2005" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;ol&gt;
&lt;li&gt;Screen layouts are now part of source code control. They get stored in the SL subdirectory of XLEXP. You will need to create the files sc_slhdr and sc_slcmnt before you can add layouts to a mod.&lt;/li&gt;
&lt;/ol&gt;
&lt;h2&gt;07 September 2005&lt;span class="hx:absolute hx:-mt-20" id="07-september-2005"&gt;&lt;/span&gt;
&lt;a href="#07-september-2005" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;ol&gt;
&lt;li&gt;There is a better way of moving mods between systems and and applications. See: &lt;a href="https://127.0.0.1:8080/docs/features/versioncontrol/rmodimport/"&gt;4C Remote Mod Imports&lt;/a&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h2&gt;18 August 2004&lt;span class="hx:absolute hx:-mt-20" id="18-august-2004"&gt;&lt;/span&gt;
&lt;a href="#18-august-2004" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;ol&gt;
&lt;li&gt;xlimport sets the sys.fh_schnum and sys.pr_schnum vars correctly. This makes it possible to xlimport files and programs that are being edited without losing the ability to edit the program or file. Please note, that if you use xlimport to import files or programs, the version control VersionFile is not imported.&lt;/li&gt;
&lt;li&gt;The program copy and file copy programs will correctly set sys.pr_schnum and sys.fh_schnum.&lt;/li&gt;
&lt;/ol&gt;</description></item><item><title>4C Remote Mod Import</title><link>https://127.0.0.1:8080/docs/features/versioncontrol/rmodimport/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://127.0.0.1:8080/docs/features/versioncontrol/rmodimport/</guid><description>
&lt;p&gt;4C allows an application on one machine to import,
compile, and install installed and approved SC mods from
another application on either the same machine or on a remote
machine.
This is accomplished by the addition of a few
4C bootstrap files and programs and some shellscripts
installed into the XLLIB/SC directory.&lt;/p&gt;
&lt;p&gt;How to configure and run remote mod imports is described in the
following sections:
&lt;a href="https://127.0.0.1:8080/docs/features/versioncontrol/rmodimport/"&gt;Quick Start&lt;/a&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;A quick start overview of how to import a mod,
assuming both machine/apps are configured correctly.
&lt;a href="https://127.0.0.1:8080/docs/features/versioncontrol/rmodimport/"&gt;File Overview&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;A quick overview of the 4C files used in
Remote Mod Import.
&lt;a href="https://127.0.0.1:8080/docs/features/versioncontrol/rmodimport/"&gt;Program Overview&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;A quick overview of the 4C programs used in
Remote Mod Import
&lt;a href="https://127.0.0.1:8080/docs/features/versioncontrol/rmodimport/"&gt;Configuration&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;How to set up a machine/app to allow importing
mods from a remote machine/app
&lt;a href="https://127.0.0.1:8080/docs/features/versioncontrol/rmodimport/"&gt;Troubleshooting&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;Some troubleshooting tips and answers to the
most frequently asked questions&lt;/li&gt;
&lt;/ul&gt;
&lt;hr&gt;
&lt;h2&gt;4C Remote Mod - Quick Start&lt;span class="hx:absolute hx:-mt-20" id="4c-remote-mod---quick-start"&gt;&lt;/span&gt;
&lt;a href="#4c-remote-mod---quick-start" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Assuming you have two machine/apps that are configured
correctly for remote mod import,
here are the steps to follow in order to import a mod:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Run Mod Imports from the main development treeview menu.&lt;/li&gt;
&lt;li&gt;Select the machine/app that you want to import a mod from, and then press rtn or the &lt;code&gt;&amp;lt;user1&amp;gt;&lt;/code&gt; function key.&lt;/li&gt;
&lt;li&gt;At the selection screen choose the range of dates that you wish to see available mods for, and then click the &lt;code&gt;&amp;lt;Show Mods&amp;gt;&lt;/code&gt; btn. You will see a list of mods that are available for import.&lt;/li&gt;
&lt;li&gt;Select the mod that you wish to import, keeping in mind that the order in which mods are imported is very important. Importing in the wrong order could result in undoing parts of a previous import.&lt;/li&gt;
&lt;li&gt;Click the &lt;code&gt;&amp;lt;Import&amp;gt;&lt;/code&gt; btn to bring up the Remote Mod Import screen.&lt;/li&gt;
&lt;li&gt;Click the &lt;code&gt;&amp;lt;Start&amp;gt;&lt;/code&gt; btn to begin the import. If prompted for a logon, enter your user name and passwd for the machine you are importing from. This is necessary because the local 4csrvr needs to run a 4c program on the remote machine in order to export the mod and copy it to your local machine.&lt;/li&gt;
&lt;li&gt;If the import succeeds, click the &lt;code&gt;&amp;lt;Continue&amp;gt;&lt;/code&gt; button to bring up the Local Mod Compile screen. If the import has warnings, review the warnings by clicking the &lt;code&gt;&amp;lt;ViewOutput&amp;gt;&lt;/code&gt; btn. If you are satisfied that the warnings can safely be ignored, then click the &lt;code&gt;&amp;lt;Continue&amp;gt;&lt;/code&gt; btn to continue the mod import. If you want to cancel the import because of the warnings, click the &lt;code&gt;&amp;lt;Cancel&amp;gt;&lt;/code&gt; btn or press the &lt;code&gt;&amp;lt;Cancel&amp;gt;&lt;/code&gt; function key. If the import has errors, click the &lt;code&gt;&amp;lt;ViewOutput&amp;gt;&lt;/code&gt; btn to view the error messages. You will not be able to continue a mod import that has errors. You must exit and restart the mod import after fixing the problem that caused the errors..&lt;/li&gt;
&lt;li&gt;At the Local Mod Compile screen, click the &lt;code&gt;&amp;lt;Start&amp;gt;&lt;/code&gt; btn to start the compile of all files and programs in the mod.&lt;/li&gt;
&lt;li&gt;If the compile succeeds on all files and programs in the mod, click the &lt;code&gt;&amp;lt;Continue&amp;gt;&lt;/code&gt; btn to bring up the Local Mod Install screen. If the compile failed on any program or file, you will not be able to continue to the Local Mod Install screen. You can click &lt;code&gt;&amp;lt;ViewOutput&amp;gt;&lt;/code&gt; to view the compile errors, or you can click &lt;code&gt;&amp;lt;Exit&amp;gt;&lt;/code&gt; to return to the Remote Mod List.&lt;/li&gt;
&lt;li&gt;At the Local Mod Install screen, click &lt;code&gt;&amp;lt;Start&amp;gt;&lt;/code&gt; to begin the mod install.&lt;/li&gt;
&lt;li&gt;If the mod installs successfully, click &lt;code&gt;&amp;lt;Exit&amp;gt;&lt;/code&gt; to return to the Remote Mod List.&lt;/li&gt;
&lt;li&gt;There should not be any reason for the Local Mod Install to fail as long as the 4C Version Control has been configured correctly.&lt;/li&gt;
&lt;li&gt;Back at the Remote Mod List screen you can select another mod to import and repeat the above steps.&lt;/li&gt;
&lt;/ul&gt;
&lt;hr&gt;
&lt;h2&gt;4C Remote Mod Import - File Overview&lt;span class="hx:absolute hx:-mt-20" id="4c-remote-mod-import---file-overview"&gt;&lt;/span&gt;
&lt;a href="#4c-remote-mod-import---file-overview" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;The new files are:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;sc_impcntl - defines the remote machine/application that you will import from.&lt;/li&gt;
&lt;li&gt;sc_import - keeps a rcd of mods imported.&lt;/li&gt;
&lt;/ul&gt;
&lt;hr&gt;
&lt;h2&gt;4C Remote Mod Import - Program Overview&lt;span class="hx:absolute hx:-mt-20" id="4c-remote-mod-import---program-overview"&gt;&lt;/span&gt;
&lt;a href="#4c-remote-mod-import---program-overview" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;The new programs are:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;sc.impcntl.fm - Maintains the sc_import file and links to other programs that allow you to view available mods to import as well as mods already imported. This program is accessed from the Mod Import entry on the system treeview menu. Most tasks associated with imported mods are accessible through this program.&lt;/li&gt;
&lt;li&gt;sc.imp.purge - Simple program for purging old import data. This program is run up to once a day automatically by sc.impcntl.fm. Currently, sc_import rcds are never purged, just data associated with exporting a mod from a remote system to the local system, extracting the programs and files, and installing the mod locally.&lt;/li&gt;
&lt;li&gt;sc.rmod.s - Called from sc.impcntl.fm, this program allows you to select a range of dates and some other criteria for viewing importable and imported mods.&lt;/li&gt;
&lt;li&gt;sc.rmod.1 - Called from sc.rmod.s. Depending on the selection criteria selected in sc.rmod.s, this program shows you some or all of the following: Remote Mods not yet imported Remote Mods that have been imported but not installed Remote Mods that have been imported and installed. From this program you can: Import a mod that has not been imported yet Compile a mod that has already been imported Install a mod that has been imported and compiled View the output of a mod import.&lt;/li&gt;
&lt;li&gt;sc.rmod.imp - Called when you click Import in sc.rmod.1 This program leads you through importing a remote mod. About all you need to do is read the messages and click the Start/Continue/ViewOutput buttons. Errors or Warnings may be generated. You should be real familiar with warnings and errors that can be generated during a mod import and only continue after careful review. The errors and warnings are described in the Mod Import Conflicts section of &lt;a href="https://127.0.0.1:8080/docs/features/versioncontrol/vc/"&gt;Version Control&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;sc.rmod.imp.bld - Called from either sc.rmod.1 when Compile is clicked or from sc.rmod.imp when Continue is clicked after a mod has been successfully imported. Errors generated during compile will be saved. This program will not run if the mod was not successfully imported.&lt;/li&gt;
&lt;li&gt;sc.rmod.imp.inst - Called from either sc.rmod.1 when Install is clicked, or from sc.rmod.imp.bld after a successful compile and you click Continue during a mod import. This program will not run if the mod was not successfully imported or if it has not been successfully compiled.&lt;/li&gt;
&lt;li&gt;sc.impout.view.s - Simple starter prog to start the view output program&lt;/li&gt;
&lt;li&gt;sc.impout.view.1 - Displays one of the output viewer tabs&lt;/li&gt;
&lt;/ul&gt;
&lt;hr&gt;
&lt;h2&gt;4C Remote Mod Import - Configuration&lt;span class="hx:absolute hx:-mt-20" id="4c-remote-mod-import---configuration"&gt;&lt;/span&gt;
&lt;a href="#4c-remote-mod-import---configuration" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Before you can configure remote mod import,
both machine/apps need to have
&lt;a href="https://127.0.0.1:8080/docs/features/versioncontrol/vc/"&gt;4C Version Control&lt;/a&gt;
configured correctly.&lt;/p&gt;
&lt;p&gt;Configuring an application to use remote import is simply a matter
of creating the 2 new files, sc_impcntl and sc_import in the application
directory and definining one or more remote mod/app combinations that
you want to use for remote mod import.&lt;/p&gt;
&lt;p&gt;Here is how you do it:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Log into the local machine/app where you want to import to.&lt;/li&gt;
&lt;li&gt;Run the sc.impcntl.fm program. Do this by double clicking Mod Imports from the system treeview menu. If this is the first time this program has been run for this application, then you will be prompted to create sc_impcntl and sc_import. You should answer &amp;ldquo;y&amp;rdquo; to both create prompts. You will now see a list of current sc_impcntl rcds. You can add new rcds using normal 4C methods. The fields that you need to set for each new sc_impcntl rcd are as follows: scic_name - specify a simple name that indicates the machine and app that will be used for remote mod imports. scic_desc - long description scic_connectstr - A normal 4C connection string in the format Machine=&lt;code&gt;&amp;lt;hostname&amp;gt;&lt;/code&gt;,Port=&lt;code&gt;&amp;lt;portno&amp;gt;&lt;/code&gt;,Type=TCP,App=&lt;code&gt;&amp;lt;app&amp;gt;&lt;/code&gt; It is important that &lt;code&gt;&amp;lt;app&amp;gt;&lt;/code&gt; specifies the remote application exactly as it is in the _4CSRVRCONFIG file on the remote machine. It is case sensitive. scic_keepfor - The number of days to keep the mod import data. This number can be between 7 and 30. For an import that completes with no errors or warnings, there is very little data that is kept until purge. When a mod import has errors or warnings, more data is kept to aid in debugging if necessary. All data kept is stored as text files in the scic_dir. The sc_import file is never purged. scic_dir - The directory to keep import data and import temp files. This directory will be created when first needed, but you should check that permissions are the way you want them. It is safe to use the same directory for multiple sc_impcntl rcds belonging to the same app, but you should not use the same directory for different apps. You may wish to use an application subdirectory for this such as: /usr/4capp/Imports or you may prefer something like: /tmp/Imports/appname This directory will be created the first time you try to import a mod, but depending on the way you have permissions set on your system, it could be created with the wrong permissions. You need to verify that the permissions are correct and that any user who will import mods has sufficient privileges to create subdirectories, create files, read files, and write files in that directory. scic_usr - A convenience that combined with scic_epwd allows you to run the import without being prompted for a logon to the remote machine. scic_epwd - Encrypted PWD (use encode4c &lt;code&gt;&amp;lt;pwd&amp;gt;&lt;/code&gt; to get and encrypted pwd) Do not use this unless your machines are completely isolated from the rest of the world. The reason a logon is required is that the local machine must start a 4ccl local process that connects to the remote machine 4csrvr and run 4C programs that export the mod and copy the mod export file to the local machine. If you do not specify Usr/EPwd, then every time you try to import a mod you will be prompted for a usr/pwd combination. After configuring your app to use remote mod import, you should test it with a safe mod with only a single unimportant temp program or file that has no effect on the running of your application.&lt;/li&gt;
&lt;/ul&gt;
&lt;hr&gt;
&lt;h2&gt;4C Remote Mod Import - Troubleshooting&lt;span class="hx:absolute hx:-mt-20" id="4c-remote-mod-import---troubleshooting"&gt;&lt;/span&gt;
&lt;a href="#4c-remote-mod-import---troubleshooting" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;ol&gt;
&lt;li&gt;Problem: You see the wrong list of mods when viewing the remote mods Check the connection string in the sc_import file and make sure that the App=&lt;code&gt;&amp;lt;app&amp;gt;&lt;/code&gt; is specified exactly as it is in the _4CSRVRCONFIG file on the remote machine. Check and make sure that the user running 4cfs on the remote machine has permission to run the application specified in the sc_impcntl connection string. This user could be the default user specified in the _4CSRVRCONFIG on the remote machine.&lt;/li&gt;
&lt;/ol&gt;</description></item><item><title>4C Version Control</title><link>https://127.0.0.1:8080/docs/features/versioncontrol/vc/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://127.0.0.1:8080/docs/features/versioncontrol/vc/</guid><description>
&lt;p&gt;Version 2.3 of June 1994 implements version control for 4C file
and program specifications.
Using 4C Version Control, you can:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Archive individual program or file specifications in either RCS or SCCS format.&lt;/li&gt;
&lt;li&gt;Maintain a record of changes made to program or file specifications.&lt;/li&gt;
&lt;li&gt;Compare 2 versions of program or file specifications.&lt;/li&gt;
&lt;li&gt;Backup to an earlier version of a program or file.&lt;/li&gt;
&lt;li&gt;Checkpoint Current changes.&lt;/li&gt;
&lt;li&gt;Install changes to a program or file specs.&lt;/li&gt;
&lt;li&gt;Document changes made to program or file specs.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;4C Version Control does not try to allow everything that SCCS or RCS
may allow.
In particular, branching off is not supported.
4C Version Control works in a linear fashion, using a release number
and a level number.
This will be explained in detail later.&lt;/p&gt;
&lt;hr&gt;
&lt;h2&gt;4C Version Control - Requirements&lt;span class="hx:absolute hx:-mt-20" id="4c-version-control---requirements"&gt;&lt;/span&gt;
&lt;a href="#4c-version-control---requirements" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;In order to use 4C Version Control, you need:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Either SCCS or RCS. Most SystemV implementations include SCCS as part of Unix development. RCS can be gotten from the Free Software Foundation (FSF) and is included in the 4C distribution tape. Please read the accompanying licence agreement from the FSF.&lt;/li&gt;
&lt;li&gt;Support for long filenames. The name of the stored files will be program or file name + 2 characters.&lt;/li&gt;
&lt;li&gt;The utility programs sed, diff, and awk. FSF implementations of theses utilities will be included at a later date.&lt;/li&gt;
&lt;/ol&gt;
&lt;hr&gt;
&lt;h2&gt;4C Version Control - Setting Up&lt;span class="hx:absolute hx:-mt-20" id="4c-version-control---setting-up"&gt;&lt;/span&gt;
&lt;a href="#4c-version-control---setting-up" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;4C Version Control is implemented on a per directory basis.
Each 4C specification directory can be set to use or not
use version control.
Each directory can also independently specify whether to use
RCS or SCCS.
In addition, for each directory that uses version control,
specific categories can be disabled from using version control.
In order to start using 4C Version Control, do the following
for each 4C spec directory you wish to use version control with.&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Make sure that a sys.config file exists in the 4C Spec directory. If one does not, create it with the following command: xlcr -f sys.config -n &lt;code&gt;&amp;lt;4cDir&amp;gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;Run the program sys.conf.fm. This can be run from the System Environment menu. Enable version control by specifying &amp;ldquo;y&amp;rdquo; at the enable prompt and then specify either RCS or SCCS for version control type.&lt;/li&gt;
&lt;li&gt;Select the categories that you want to disable version control for. Typically, a category such as tmp would be disabled from using version control. Set sys.catg_ignsccf to &amp;ldquo;y&amp;rdquo; for these categories by running the sys.catg.maint program from the environment menu.&lt;/li&gt;
&lt;li&gt;Make sure that the RCS or SCCS binary programs are in your search PATH.&lt;/li&gt;
&lt;li&gt;Make sure that the XLEXP environment variable is set and exported. This should be set to the directory name where the 4C specs are to be exported. It can be set to your 4C spec directory, or another directory. In any case, there will be more directories created below this one to store the RCS or SCCS files. You should make sure that there will be plenty of space to hold the exported programs and files. Initially, the amount of space needed will be less than that needed by the 4C specs in JISAM format. However, as changes are made and saved, the amount of space needed will increase.&lt;/li&gt;
&lt;li&gt;Create the Version Control Bootstrap files. These MUST exist in your 4C Spec directory. If they do not, then you will be adding records to the ones in the bootstrap spec directory. These will most likely be overwritten the next time the bootstrap programs are installed. The following commands will create the files you need: &lt;code&gt;xlcr -f sc_cntl -n &lt;/code&gt;&amp;lt;4cDir&amp;gt;&lt;code&gt;xlcr -f sc_hdr -n&lt;/code&gt;&amp;lt;4cDir&amp;gt;&lt;code&gt;xlcr -f sc_prog -n&lt;/code&gt;&amp;lt;4cDir&amp;gt;&lt;code&gt;xlcr -f sc_file -n&lt;/code&gt;&amp;lt;4cDir&amp;gt;&lt;code&gt;xlcr -f sc_slhdr -n&lt;/code&gt;&amp;lt;4cDir&amp;gt;&lt;code&gt;xlcr -f sc_prcmnt -n&lt;/code&gt;&amp;lt;4cDir&amp;gt;&lt;code&gt;xlcr -f sc_flcmnt -n&lt;/code&gt;&amp;lt;4cDir&amp;gt;&lt;code&gt;xlcr -f sc_slcmnt -n&lt;/code&gt;&amp;lt;4cDir&amp;gt;``&lt;/li&gt;
&lt;li&gt;Export All your file definitions and program definitions to create the initial RCS or SCCS files. This is done by running the program sc.file.create for files, and the program sc.prog.create for programs. This should only be done once for each program or file. You can export all programs or files at once, or by category name. Exporting all programs can take a bit of time. The 2 export programs are on the SC Utility program menu.&lt;/li&gt;
&lt;li&gt;Verify that the exports worked. This can be done by looking in the file $XLEXP/PROGRAM/SC/scm00000 after the export is done. The sc.file.create and sc.prog.create programs will prompt if you want to see this output. In the case of problems, you can remove the directories $XLEXP/PROGRAM and $XLEXP/FILE and start all over. It is probably a good idea to try this on 1 or 2 files and 1 or 2 programs before exporting an entire category. It is possible to get warnings about a fldsep found in a file. This will happen if any of your alpha fields have an embedded CntlA in them. In doing the export, when this is found, the character is changed to a question mark in the exported file. You can check for these ahead of time using xlflgrep with the -U option. If you expect to leave an overnight export, this is a good idea. The file I have found the most CntlAs in has been sys.dpy_field. They also seem to exist in sys.pcl_stmt, due to the old xled program allowing you to type them. So at the very least, run the commands: &lt;code&gt;xlflgrep -f sys.dpy_field -m &amp;quot;^A&amp;quot; -r &amp;quot;&amp;quot; -G -U xlflgrep -f sys.pcl_stmt -m &amp;quot;^A&amp;quot; -r &amp;quot;&amp;quot; -G -U NOTE: the first quoted string is literally doublequote CntlA doublequote&lt;/code&gt; The CntlA may or may not echo when typed. For most shells, the way you type the CntlA is by pressing CntlV then CntlA.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Assuming all went well, you can now use the 4C Version Control system.
It will probably be helpful to familiarize yourself with the files
used by 4C version control as well as the files
in the directories beneath $XLEXP/PROGRAM and $XLEXP/FILE first.&lt;/p&gt;
&lt;hr&gt;
&lt;h2&gt;4C Version Control - Files&lt;span class="hx:absolute hx:-mt-20" id="4c-version-control---files"&gt;&lt;/span&gt;
&lt;a href="#4c-version-control---files" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;The following files are used by the Version Control System and are
regular ISAM files.&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Uses one rcd with key &amp;ldquo;SC&amp;rdquo; to maintain next mod#. This rcd will be created if it does not exist, the first time you add a mod.&lt;/li&gt;
&lt;li&gt;The Modification header file. Each sc_hdr corresponds to one 4C modification. A modification will typically involve multiple programs and files.&lt;/li&gt;
&lt;li&gt;The program detail rcds for a particular modification There can be 0,1 or many sc_prog rcds per 4C mod.&lt;/li&gt;
&lt;li&gt;Detailed comments for a particular program for a particular 4C mod.&lt;/li&gt;
&lt;li&gt;The file detail rcds for a particular modification There can be 0,1 or many sc_file rcds per 4C mod.&lt;/li&gt;
&lt;li&gt;Detailed comments for a particular file for a particular 4C mod.&lt;/li&gt;
&lt;li&gt;The screen layout detail rcds for a particular modification There can be 0,1 or many sc_slhdr rcds per 4C mod.&lt;/li&gt;
&lt;li&gt;Detailed comments for a particular screen layout for a particular 4C mod.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;In addition to these files, the following sequential files and
directories are also used.&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;The 4C work directory for checked out RCS or SCCS program specs.&lt;/li&gt;
&lt;li&gt;The directory where the SCCS files are created for program specs.&lt;/li&gt;
&lt;li&gt;The directory where the RCS files are created for program specs.&lt;/li&gt;
&lt;li&gt;The 4C work directory for checked out RCS or SCCS file specs.&lt;/li&gt;
&lt;li&gt;The directory where the SCCS files are created for file specs.&lt;/li&gt;
&lt;li&gt;The directory where the RCS files are created for file specs.&lt;/li&gt;
&lt;li&gt;The directory of output files for all SCCS or RCS cmds executed for a particular mod. Files in this directory will have a name scmNNNNN where NNNNN refers to a mod#. These files can be viewed with a regular text editor.&lt;/li&gt;
&lt;/ol&gt;
&lt;hr&gt;
&lt;h2&gt;4C Version Control - Work &amp;amp; Version Files&lt;span class="hx:absolute hx:-mt-20" id="4c-version-control---work--version-files"&gt;&lt;/span&gt;
&lt;a href="#4c-version-control---work--version-files" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;4C uses a shell script to facilitate all
RCS commands and SCCS commands.
The shell script used for RCS is 4cRcs,
and the shell script used for SCCS is
4cSccs.
Both shell scripts take the same arguments,
and both are located in the XLBIN directory.
These shell scripts will not be documented in detail
since all calls to them are thru 4C bootstrap programs.&lt;/p&gt;
&lt;p&gt;4C creates a WorkFile thru
sc.file.create.1 or sc.prog.create.1.
This WorkFile is a sequential file with
all of the 4C spec records for 1 file or 1 program in it.
It is with the WorkFile,
that RCS and SCCS can work.
They cannot work directly on the 4C bootstrap files.
Currently, none of the help or documentation rcds associated
with a file or program are stored in the WorkFile.&lt;/p&gt;
&lt;p&gt;When doing the initial file or program export,
or when doing an install on a new program or file,
a VersionFile is created from the WorkFile.
The VersionFile is the file found in the RCS or
the SCCS subdirectories.
The VersionFile stores all changes to the WorkFile
as well as appropriate comments regarding changes.
It is not necessary to know anything more about the
VersionFile.
However, the RCS or SCCS documentation can
be read if you want a better understanding of them.&lt;/p&gt;
&lt;p&gt;The VersionFile is kept always,
but 4C does not need the WorkFile.
It is normally removed by the 4cRcs or 4cSccs shell script.
It can also always be recreated from the 4C specs
if it is being modified,
or from the VersionFile if it is not checked out.&lt;/p&gt;
&lt;p&gt;4C Still runs off of the bootstrap files when
running programs,
compiling programs,
or modifying programs.
The VersionFile is only used for maintaining the changes to
a program or file.
The current version is always in the bootstrap files.&lt;/p&gt;
&lt;hr&gt;
&lt;h2&gt;4C Version Control - CheckOut&lt;span class="hx:absolute hx:-mt-20" id="4c-version-control---checkout"&gt;&lt;/span&gt;
&lt;a href="#4c-version-control---checkout" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;In order to modify a program or file definition now,
it must be checked out from RCS or SCCS.
You check out a program or file by adding it to the
appropriate mod.
4C then calls the correct script file to check it out from SCCS or RCS
and lock it.
Another term for checked out is edited.&lt;/p&gt;
&lt;p&gt;When a program is checked out,
you can modify it as always.
Your changes are reflected immediately in the 4C spec files,
but they are not reflected in the VersionFile until
you install them.
Once a program or file mod is installed,
you have a permanent rcd of them.
You can always go back to an earlier installed version of
a file or program if necessary.&lt;/p&gt;
&lt;p&gt;When a program or file is checked out,
no one else can check it out until it has been
installed or unedited.
This is accomplished by storing the mod# in the
appropriate sys.program or sys.file_hdr rcd.
No program or file with a mod# set can be added to another mod.&lt;/p&gt;
&lt;p&gt;Each mod allows a single line comment to indicate the
general reason for the mod.
In addition, each program or file on the mod
allows multi line comments documenting the change.
These comments are useful when running the mod reports.&lt;/p&gt;
&lt;hr&gt;
&lt;h2&gt;4C Version Control - Installing Changes&lt;span class="hx:absolute hx:-mt-20" id="4c-version-control---installing-changes"&gt;&lt;/span&gt;
&lt;a href="#4c-version-control---installing-changes" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;A program or file can be installed when the entire mod is installed
or individually.
In either case,
installing involves creating the WorkFile
and then calling the appropriate shell script to
to update the VersionFile from the WorkFile
and to unlock the VersionFile.
The comments for each prog or file as well as the mod#
and the mod comment are stored in the VersionFile also.
This is accomplished thru bootstrap programs.
All that you need to do is press the appropriate
function key.&lt;/p&gt;
&lt;hr&gt;
&lt;h2&gt;4C Version Control - Checkpoints&lt;span class="hx:absolute hx:-mt-20" id="4c-version-control---checkpoints"&gt;&lt;/span&gt;
&lt;a href="#4c-version-control---checkpoints" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;It is possible to save changes to your
program or file specs temporarily.
This is done by checkpointing the file or program,
or an entire mod.
Only a program or file that is checked out can
be checkpointed.
When you checkpoint a file or program, 4C
creates the WorkFile from the bootstrap specifications.
It then renames this WorkFile to be ~programname or
~filename.
It is necessary to do this since there are other
times that the WorkFile is created and removed.
While working on a mod, it is always possible
to back out changes to a file or program
to the last checkpoint.
When you first add a program or file to a mod,
it is checkpointed automatically.
Every time you create a new checkpoint,
you lose the last checkpoint for that file or program.
Checkpoints are temporary,
Installs are permanent.&lt;/p&gt;
&lt;hr&gt;
&lt;h2&gt;4C Version Control - UndoingChanges&lt;span class="hx:absolute hx:-mt-20" id="4c-version-control---undoingchanges"&gt;&lt;/span&gt;
&lt;a href="#4c-version-control---undoingchanges" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;4C Version Control gives you several ways
to backout changes.&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;For a program or file being edited, you can back out ALL changes to that file or program. This gets the previous version of the program or file from the VersionFile and imports it into the 4C spec files. This is called unediting. Unediting also unlocks the VersionFile and sets the mod# to blank in the sys.file_hdr or sys.program rcd. You can only unedit a program or file that is checked out and that has a VersionFile. If this is a new file or program, the VersionFile will not exist yet. Unediting is accomplished by specifying ALL changes to the undo dialog. If a program or file is unedited, it can be reedited, or deleted from the mod.&lt;/li&gt;
&lt;li&gt;You can undo changes on an edited program or file up to the last checkpoint also. This is also done thru the undo dialog. Undoing changes up to a checkpoint does not unedit the program or file. You can continue making changes to it.&lt;/li&gt;
&lt;li&gt;Lastly, you can undo changes to a file or program by getting an earlier installed version of the file or program. In order to do this you MUST: Make sure the file or program is currently edited. Call up the GetOld dialog and select the version that you want to get. The file or program will remain edited until you install or unedit it. Install it. This will create a new version and will not delete any of the other installed versions.&lt;/li&gt;
&lt;/ol&gt;
&lt;hr&gt;
&lt;h2&gt;4C Version Control - Guide Lines&lt;span class="hx:absolute hx:-mt-20" id="4c-version-control---guide-lines"&gt;&lt;/span&gt;
&lt;a href="#4c-version-control---guide-lines" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;4C Version Control is not fool proof.
You can bypass the controls and defeat the purpose of using
it.
By following a few simple guidelines,
you should have no problems with it.&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Either always use Version Control or never use it. Do not turn it off and then back on again.&lt;/li&gt;
&lt;li&gt;Do not use xlexport and xlimport to move programs between systems that are using Version Control. You can use xlexport and xlimport to move programs from systems using Version Control to systems not using Version Control. Instead, use the Mod Export utility from the SC Utility menu to export INSTALLED mods from one system, and use the scimport shell script to import them on the other system. This is documented in detail elsewhere. Another alternative to xlexport and xlimport is to set up and use Remote Mod Imports. This is documented elsewhere. In the Cl/Srvr version 4.0.4 and higher, you can use xlimport on systems using Version Control, but this should only be done when you do not need to import the VersionFile.&lt;/li&gt;
&lt;li&gt;If you must use any 4C utility program to make changes to the bootstrap files, make sure that you put those files or programs on a mod by building a mod list and then importing that list to the appropriate mod. This can be useful especially when xlflgrep is used to make many changes. Using the mod list build and mod list import is documented elsewhere.&lt;/li&gt;
&lt;li&gt;Do not use the copy or copy/paste utilities to copy any of the main hdr files (Characteristics: sys.program, sys.file_hdr, sl_hdr) to a program/file/layout that is being edited. You can copy to programs/files/layouts that are not being edited and then add them to a mod. In the Cl/Srvr version 4.0.4 and higher, you can use program and file copy on systems using Version Control, but this should only be done when you do not need to copy the VersionFile.&lt;/li&gt;
&lt;li&gt;Do not delete the program/file/layout main hdr rcd for a program/file/layout that is being edited. If you must do, this, you should first unedit (Undo All) the file/program/ layout.&lt;/li&gt;
&lt;li&gt;REMEMBER: Changes that are made but not ever put in the VersionFile WILL GET LOST.&lt;/li&gt;
&lt;/ol&gt;
&lt;hr&gt;
&lt;h2&gt;4C Version Control - Mod Export&lt;span class="hx:absolute hx:-mt-20" id="4c-version-control---mod-export"&gt;&lt;/span&gt;
&lt;a href="#4c-version-control---mod-export" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Installed mods can be exported to a single export file.
Multiple mods can be exported to the same export file
as long as the export is done at the same time.
Once a mod is exported,
it can be imported on another system.
It is not necessary that both systems be using RCS
or both systems be using SCCS.
They can be mixed.
In order to export 1 or more INSTALLED mods,
do the following:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Run sc.mod.exp from the SC Utility Menu.&lt;/li&gt;
&lt;li&gt;Select installed mods by date and edit the list if you like.&lt;/li&gt;
&lt;li&gt;Enter the name of the sequential file to export the mods to. If the file exists, you must clear it or select another file.&lt;/li&gt;
&lt;li&gt;Enter &amp;ldquo;y&amp;rdquo; at the &amp;ldquo;Are you sure prompt&amp;rdquo;.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This will create a sequential file with the following information.&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;1 line indicating if the VersionFiles are in SCCS or RCS format.&lt;/li&gt;
&lt;li&gt;All VersionFiles for program specs and file specs. Each VersionFile is preceded by a ProgramStart or FileStart record and is followed by a ProgramEnd or a FileEnd record. This will be the bulk of the file.&lt;/li&gt;
&lt;li&gt;FileStart records indicating the beginning of a VersionFile for a file. The FileStart record indicates the file name, the lowest version num exported, and the mod# of with that version of the file.&lt;/li&gt;
&lt;li&gt;FileEnd rcds indicating the end of 1 file VersionFile.&lt;/li&gt;
&lt;li&gt;ProgramStart records indicating the beginning of a VersionFile for a program. The ProgramStart record indicates the program name, the lowest version num exported, and the mod# of with that version of the program.&lt;/li&gt;
&lt;li&gt;ProgramEnd rcds indicating the end of 1 program VersionFile.&lt;/li&gt;
&lt;li&gt;The VersionFiles are followed by the sc rcds that make up the mods exported.&lt;/li&gt;
&lt;li&gt;Next are ImpFile and ImpProgram records. These indicate program or file name, version to import, and the mod# that they were exported from.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This mod export file can be viewed with a normal text editor,
but it does have control characters in it, so it must
be treated as a binary file when transfering it to other systems.&lt;/p&gt;
&lt;p&gt;Usually it is possible to use different export files for each
mod, but if two different mods both have programA and programB
and 1 of them installed programA first and the other
installed programB first, then those 2 mods must be exported
together.
All mods exported to the same export file must be imported
at the same time.
There is no way to separate them.&lt;/p&gt;
&lt;p&gt;From either sc.fm.active or sc.browse you can export a single
mod.&lt;/p&gt;
&lt;hr&gt;
&lt;h2&gt;4C Version Control - Mod Import&lt;span class="hx:absolute hx:-mt-20" id="4c-version-control---mod-import"&gt;&lt;/span&gt;
&lt;a href="#4c-version-control---mod-import" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Once you have moved the mod export file to another system
it can be imported using a shell script, scimport.
For a list of options type:&lt;/p&gt;
&lt;div class="hextra-code-block hx:relative hx:mt-6 hx:first:mt-0 hx:group/code"&gt;
&lt;div&gt;&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;scimport -help&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;/div&gt;&lt;div class="hextra-code-copy-btn-container hx:opacity-0 hx:transition hx:group-hover/code:opacity-100 hx:flex hx:gap-1 hx:absolute hx:m-[11px] hx:right-0 hx:top-0"&gt;
&lt;button
class="hextra-code-copy-btn hx:group/copybtn hx:cursor-pointer hx:transition-all hx:active:opacity-50 hx:bg-primary-700/5 hx:border hx:border-black/5 hx:text-gray-600 hx:hover:text-gray-900 hx:rounded-md hx:p-1.5 hx:dark:bg-primary-300/10 hx:dark:border-white/10 hx:dark:text-gray-400 hx:dark:hover:text-gray-50"
title="Copy code"
aria-label="Copy code"
data-copied-label="Copied!"
&gt;
&lt;div class="hextra-copy-icon hx:group-[.copied]/copybtn:hidden hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;div class="hextra-success-icon hx:hidden hx:group-[.copied]/copybtn:block hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;/button&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;p&gt;The easiest way to use scimport is to first make sure
that the env vars XLLIB and XLTMP are set correctly.
XLTMP will default to /tmp if not set but there is no
default for XLLIB.
XLLIB should be set to the directory with all of the sc shell scripts,
usually /usr/4c/lib.
If the mods in the mod export file were installed and
should be added to a new mod on the system they are being imported
to then just enter the following cmd:&lt;/p&gt;
&lt;div class="hextra-code-block hx:relative hx:mt-6 hx:first:mt-0 hx:group/code"&gt;
&lt;div&gt;&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;scimport `&amp;lt;mexpfile&amp;gt;`&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;/div&gt;&lt;div class="hextra-code-copy-btn-container hx:opacity-0 hx:transition hx:group-hover/code:opacity-100 hx:flex hx:gap-1 hx:absolute hx:m-[11px] hx:right-0 hx:top-0"&gt;
&lt;button
class="hextra-code-copy-btn hx:group/copybtn hx:cursor-pointer hx:transition-all hx:active:opacity-50 hx:bg-primary-700/5 hx:border hx:border-black/5 hx:text-gray-600 hx:hover:text-gray-900 hx:rounded-md hx:p-1.5 hx:dark:bg-primary-300/10 hx:dark:border-white/10 hx:dark:text-gray-400 hx:dark:hover:text-gray-50"
title="Copy code"
aria-label="Copy code"
data-copied-label="Copied!"
&gt;
&lt;div class="hextra-copy-icon hx:group-[.copied]/copybtn:hidden hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;div class="hextra-success-icon hx:hidden hx:group-[.copied]/copybtn:block hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;/button&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;hr&gt;
&lt;h2&gt;4C Version Control - Multiple Machines&lt;span class="hx:absolute hx:-mt-20" id="4c-version-control---multiple-machines"&gt;&lt;/span&gt;
&lt;a href="#4c-version-control---multiple-machines" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;I will now describe two scenarios for maintaining your 4C source
on multiple machines.
Both will work.
The first is what I call a master/slave method.
Using the master/slave method, 1 machine is designated the
master and all others are slaves.
To use this method, do the following:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Designated 1 machine the master.&lt;/li&gt;
&lt;li&gt;Make sure that the source on the master is current and that all of it is in SC Installed format.&lt;/li&gt;
&lt;li&gt;When a source code change needs to be made, create a new mod on the master machine, and add the appropriate files and programs to the mod. You can add comments at this point.&lt;/li&gt;
&lt;li&gt;DO NOT CHANGE ANY PROGRAM or FILE on the master machine.&lt;/li&gt;
&lt;li&gt;From sc.fm.active, export this mod only. The source that is being exported is the source in the version file, not the source in the isam files. That is why it is necessary to avoid making changes on the master machine.&lt;/li&gt;
&lt;li&gt;Move the newly created export file to the slave machine.&lt;/li&gt;
&lt;li&gt;Import the mod onto the slave machine using the following cmd: &lt;code&gt;scimport -merge &lt;/code&gt;&lt;mexpfile&gt;`` The -merge option will prevent the imported mod from being installed. Since no mod# was specified, scimport will create a new mod on the slave machine.&lt;/li&gt;
&lt;li&gt;Make all source code changes on the slave machine.&lt;/li&gt;
&lt;li&gt;Add all comments to the sc files on the slave machine. Once a program or file is in a mod on the slave machine, you should only add comments on the slave machine. Initial comments can be done on the master machine at the time the program or file is added to the mod.&lt;/li&gt;
&lt;li&gt;When your mod is complete, install it on the slave machine.&lt;/li&gt;
&lt;li&gt;Export the Installed mod from the slave machine to a new mexpfile.&lt;/li&gt;
&lt;li&gt;Move the mexpfile to the master machine.&lt;/li&gt;
&lt;li&gt;Import the mod onto the master machine with the following cmd: &lt;code&gt;scimport -overwrite -mod &amp;lt;mod#&amp;gt; &lt;/code&gt;&lt;mexpfile&gt;`` You MUST specify the same mod# that the mod was created with on the master machine. If this mod# is specified wrong, then the integrity of the master machine is compromised. This mod will not yet be installed on the master.&lt;/li&gt;
&lt;li&gt;Next, run 4C and compile the files and progs in your mod.&lt;/li&gt;
&lt;li&gt;Lastly, install your mod.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;While working on the mod on your slave machine, you will probably
need to add additional files or programs to the mod.
The following steps will allow you to do this.&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Go back to the master machine and add the files or programs to your mod.&lt;/li&gt;
&lt;li&gt;Re export the mod.&lt;/li&gt;
&lt;li&gt;Move the &lt;code&gt;&amp;lt;mexpfile&amp;gt;&lt;/code&gt; to the slave machine.&lt;/li&gt;
&lt;li&gt;Import the mod with the following cmd: &lt;code&gt;scimport -merge -mod &amp;lt;mod#&amp;gt; &lt;/code&gt;&lt;mexpfile&gt;`` It is absolutely imperative that you specify merge and the mod# of the mod on the SLAVE machine. Since you specified merge, the programs or files that are already on your mod will not be imported. Only programs or files that are not on your mod will be imported.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Using this method, there is a problem with unediting and
re exporting.
UNTIL the problem is fixed, if you need to unedit a program/file or
mod, you MUST do so on both the slave and on the master.
You should also delete any program or file rcds
that have been unedited from both machines.
If you need to add the program/file after unediting and deleting,
do so as described above.&lt;/p&gt;
&lt;p&gt;This method of source code control should be used where
you may have many programmers and the probability of
multiple programmers wanting to change the same source
is high.
The disadvantages of using this method of controlling source
on multiple machines are:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;It is awkward, requiring you to always have access to the master machine.&lt;/li&gt;
&lt;li&gt;It requires you to remember the mod#, which is probably different on two different machines and to specify the correct one when doing your imports.&lt;/li&gt;
&lt;li&gt;If the above rules are not followed exactly, you will compromise the integrity of your source.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The advantages of using this method of controlling source
on multiple machines are:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;You avoid any possibility of conflict since everyone will always check out src from the master machine before modifying it.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The other method I will call the multiple master method.
In this method all machines have equal status as masters.
In order to use this method, do the following:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Make sure that ALL Machines have a current copy of the source and it has ALL been installed into SC format.&lt;/li&gt;
&lt;li&gt;When you want to make a source code change, create a new mod on your local machine. Make all changes on your local machine, including sc comments and unediting and re-editing.&lt;/li&gt;
&lt;li&gt;When you are done, install your mod on your local system.&lt;/li&gt;
&lt;li&gt;Export your mod to an &lt;code&gt;&amp;lt;mexpfile&amp;gt;&lt;/code&gt;. You can do the export from sc.browse, or from sc.fm.active. Exporting from sc.fm.active must be done immediately after the install, but you can export from sc.browse anytime. You MUST ONLY export INSTALLED mods.&lt;/li&gt;
&lt;li&gt;Move the &lt;code&gt;&amp;lt;mexpfile&amp;gt;&lt;/code&gt; to ALL of the other machines and import it with the following command: &lt;code&gt;scimport &lt;/code&gt;&lt;mexpfile&gt;`` You should pay attention to any warnings and errors. It is possible, that someone else may have been modifying the same source. If you get warnings or errors you MUST check them manually before you continue installing. The mod will not necessarily have the same mod num on each machine.&lt;/li&gt;
&lt;li&gt;Once you import the mod in this way, the programs and files have been installed, but the mod itself has not. Run 4c and compile your changes and then install the mod.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This method of source code control can be used where
there are not a lot of programmers and the probability of
conflicts is low.
The disadvantages of using this method of controlling source
on multiple machines are:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;If there are many machines, the logistics of distributing the mod export files to all of them is cumbersome.&lt;/li&gt;
&lt;li&gt;The history of mods may look different on different machines depending on the order mod export files are imported.&lt;/li&gt;
&lt;li&gt;Conflicts are possible and must be resolved.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The advantages of using this method of controlling source
on multiple machines are:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;It is easy.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;If you use this method,
it probably is a good idea to designate 1 Machine the master master
and periodically update all machines from this one.&lt;/p&gt;
&lt;p&gt;You can mix the two methods of source code control for
multiple machines as long as for any one mod you follow the
rules for the style you are using.
You must NOT mix the two styles for the same mod.&lt;/p&gt;
&lt;hr&gt;
&lt;h2&gt;4C Version Control - Remote Mod Imports&lt;span class="hx:absolute hx:-mt-20" id="4c-version-control---remote-mod-imports"&gt;&lt;/span&gt;
&lt;a href="#4c-version-control---remote-mod-imports" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Another method of moving mods between machine/apps
is to use
&lt;a href="https://127.0.0.1:8080/docs/features/versioncontrol/rmodimport/"&gt;4C Remote Mod Imports&lt;/a&gt;
You will still need to have a concept of Master and slave machines
and understand what to do when conflicts occur,
but it is much easier to use.&lt;/p&gt;
&lt;hr&gt;
&lt;h2&gt;4C Version Control - Mod Import Conflicts&lt;span class="hx:absolute hx:-mt-20" id="4c-version-control---mod-import-conflicts"&gt;&lt;/span&gt;
&lt;a href="#4c-version-control---mod-import-conflicts" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Following is a list of possible conflicts or errors
that you can get while running scimport and how to resolve them.
If you get warnings,
you will be allowed to save the warnings and
you will be prompted to continue
or not before any of your source is modified.
This gives you a chance to quit the scimport and determine
whether the warnings are significant or not.
You can run scimport multiple times on the same export file
without modifying your system just by answering no to the
continue prompt or the Import SC files prompt.
Even after importing the SC files, you
can avoid changing your source by answering no to the
Import/Install Mods? prompt.
If you Import the SC files, but not the mods,
you should delete the SC file rcds for the mods just added.&lt;/p&gt;
&lt;p&gt;If you get fatal errors, scimport will not even give you the
choice of continuing or not.
You must find a way to correct the errors before you can import
the mod.&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;There is always the possibility of permission problems and if you run into these the messages should make the solution obvious.&lt;/li&gt;
&lt;li&gt;WARNING: No Changes for &amp;lt;rel.lvl&amp;gt; WARNING: Possible Duplicate Import means that for the current file or program, there are no changes between the current version and the version being imported. It is possible that no changes were made or that this mod has already been imported. This message will not occur if you are importing with the merge option since it is expected that there would not be any changes yet.&lt;/li&gt;
&lt;li&gt;WARNING: Common Starting Pnt Found at: &amp;lt;r1.l1&amp;gt; NOT &amp;lt;r2.l2&amp;gt; WARNING: Import will undo &amp;lt;r2.l2&amp;gt; to &amp;lt;r1.l1&amp;gt; means that there were changes on the system being imported to that are not reflected in the system being imported from. If you get this message, you should verify whether or not you want to import the mod or not. You could be undoing someone elses changes that are important.&lt;/li&gt;
&lt;li&gt;WARNING: No Common Starting Point - Possible Name Conflict means that there is no version of the file/prog on the system being imported to that corresponds to any version of the file/program on the system being imported from. It is possible that 2 programmers created a new file/prog with same name and entirely different meaning. You should verify that you want to import this file/prog before allowing scimport to continue.&lt;/li&gt;
&lt;li&gt;FATAL ERROR: &lt;code&gt;&amp;lt;name&amp;gt;&lt;/code&gt; is already being edited - &amp;lt;mod#&amp;gt; not specified means that you are trying to import a program/file that is already being edited on the system being imported to. Merge or overwrite was specified, but you should specify a mod# also. You will not be allowed to import your mod if you encounter this error.&lt;/li&gt;
&lt;li&gt;FATAL ERROR: &lt;code&gt;&amp;lt;name&amp;gt;&lt;/code&gt; is already being edited by another mod means that you are trying to import a mod to a specific mod# and you specified either merge or overwrite, but the file/program is being edited by another mod. You will not be allowed to import your mod if you encounter this error.&lt;/li&gt;
&lt;li&gt;FATAL ERROR: &lt;code&gt;&amp;lt;name&amp;gt;&lt;/code&gt; is already being edited means neither merge nor overwrite was specified, and the file/prog is already being edited on the system being imported to. You will not be allowed to import your mod if you encounter this error.&lt;/li&gt;
&lt;/ol&gt;
&lt;hr&gt;
&lt;h2&gt;4C Version Control - Using&lt;span class="hx:absolute hx:-mt-20" id="4c-version-control---using"&gt;&lt;/span&gt;
&lt;a href="#4c-version-control---using" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Once you set up the 4C Version Control, the default development menu
is the Source Code Control Menu.
You can get to the System Menu from here, but I hope it will
be convenient to do most of your development work from here.
4C refers to groups of program specs and file specs being
worked on together as a mod.
Normally, programs and files in a mod are being worked on for the
same or a related reason.
You can work on multiple mods at the same time.
To work on a mod or add a new mod to the system, select the
Active Mods item from the menu.
From here you can:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Add a new mod by pressing &lt;code&gt;&amp;lt;add&amp;gt;&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;Make a mod the current mod by highlighting it.&lt;/li&gt;
&lt;li&gt;Call up prog details for current mod.&lt;/li&gt;
&lt;li&gt;Call up file details for current mod.&lt;/li&gt;
&lt;li&gt;Install ALL program and file specs for the current mod.&lt;/li&gt;
&lt;li&gt;Show ALL diffs of programs and files for the current mod.&lt;/li&gt;
&lt;li&gt;Checkpoint ALL program and file specs for the current mod.&lt;/li&gt;
&lt;li&gt;Undo ALL program and file spec changes since last checkpoint.&lt;/li&gt;
&lt;li&gt;Undo ALL program and file spec changes since start of mod.&lt;/li&gt;
&lt;li&gt;Compile ALL program and files specs of current mod.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;To continue working on a mod,
make it current and call up the program or file details for
it.
Calling up the program details for a mod displays all programs
that are part of a mod.
Pressing the &lt;code&gt;&amp;lt;add&amp;gt;&lt;/code&gt; key allows adding a new program to the current mod.
Once a program is part of a mod,
it cannot be part of another mod until it has been installed,
or unedited.
It can be installed separately or with the entire mod.&lt;/p&gt;</description></item></channel></rss>