<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>4C Language Docs – 4C Logging</title><link>https://127.0.0.1:8080/docs/features/logging/</link><description>Recent content in 4C Logging on 4C Language Docs</description><generator>Hugo -- gohugo.io</generator><language>en</language><atom:link href="https://127.0.0.1:8080/docs/features/logging/index.xml" rel="self" type="application/rss+xml"/><item><title>4C Logging Change Log</title><link>https://127.0.0.1:8080/docs/features/logging/changes/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://127.0.0.1:8080/docs/features/logging/changes/</guid><description>
&lt;p&gt;&lt;a href="https://127.0.0.1:8080/docs/features/logging/"&gt;4C Logging Home&lt;/a&gt;&lt;/p&gt;
&lt;h2&gt;26 August 2020&lt;span class="hx:absolute hx:-mt-20" id="26-august-2020"&gt;&lt;/span&gt;
&lt;a href="#26-august-2020" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;ul&gt;
&lt;li&gt;4C File logging allows you to specify &amp;ldquo;*&amp;rdquo; for the log detail filename and just a directory name for the external name of logged files making it easy to log all files in a directory. You can exclude files that you don&amp;rsquo;t want logged by specifying checking the &amp;ldquo;Ignore 4C Logging&amp;rdquo; chkbox (sys.fh_nologf = &amp;lsquo;y&amp;rsquo;) in the main file definition screen.&lt;/li&gt;
&lt;li&gt;4C File logging allows using JISAM32 or JISAM64 for the log_idx file.&lt;/li&gt;
&lt;li&gt;4C File logging allows using eiher a &amp;ldquo;Short&amp;rdquo; or &amp;ldquo;Long&amp;rdquo; naming convention for the log_data sequential files. The &amp;ldquo;Short&amp;rdquo; style will overwrite log_data files every 28 - 31 days depending on the month. The &amp;ldquo;Long&amp;rdquo; style is never automatically overwritten but should still be purged regularly.&lt;/li&gt;
&lt;li&gt;The date and time used to key the log_idx and log_idx64 files are UTC date and time. This fixes the problem of updates happening during the fall switch to std time. The 4C program that displays the log details converts this time to local time for display.&lt;/li&gt;
&lt;li&gt;Though not new, there is a program logging facility that has only recently been documented. See &lt;a href="https://127.0.0.1:8080/docs/features/logging/prlogov/"&gt;4C program logging overview&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;</description></item><item><title>4C Logging - Intro</title><link>https://127.0.0.1:8080/docs/features/logging/logintro/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://127.0.0.1:8080/docs/features/logging/logintro/</guid><description>
&lt;p&gt;&lt;a href="https://127.0.0.1:8080/docs/features/logging/"&gt;4C Logging Home&lt;/a&gt;
4C version 4.0.3 and higher allows you to log
changes to your data files.
The reason for introducing the logging capability
was to make it possible to replicated 4C files
to external database files.
There are at least several other ways that this type of
log can be used including the following:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Data Replication&lt;/li&gt;
&lt;li&gt;Crash Recovery&lt;/li&gt;
&lt;li&gt;Auditing&lt;/li&gt;
&lt;li&gt;Debugging an application&lt;/li&gt;
&lt;li&gt;Undo/Redo of logged changes&lt;/li&gt;
&lt;/ul&gt;
&lt;hr&gt;
&lt;h2&gt;4C Logging - Files&lt;span class="hx:absolute hx:-mt-20" id="4c-logging---files"&gt;&lt;/span&gt;
&lt;a href="#4c-logging---files" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;The following files are involved
in 4C logging and must exist only in the XLSYSTEM
directory and not in any application directory&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;log_hdr - One rcd exists in this file for each defined log.&lt;/li&gt;
&lt;li&gt;log_det - One rcd exists in this file for each instance a data file is being logged.&lt;/li&gt;
&lt;li&gt;log_ctl - This file is used internally by 4C to serialize access to the log files when changes are part of a transaction.&lt;/li&gt;
&lt;li&gt;log_fdhdr - maintains information about the file definition of logged files&lt;/li&gt;
&lt;li&gt;log_fddet - maintains field information about logged files.&lt;/li&gt;
&lt;li&gt;log_err - This records errors in logging.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;In addition to the above 4C also uses the
following files:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;log_idx - This is a normal 4C file and exists in each individual log directory.&lt;/li&gt;
&lt;li&gt;log_data - This is a sequential file and there is one of them for each day of the month. They are named log_data.01 through log_data.31. The log_data sequential files must exist in each individual log directory.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Each log is maintained in it&amp;rsquo;s own directory.
The log directory will contain the log_idx and
all the log_data files.
The directory and files must be created before
a particular log can be used.
The newlog.sh script can be used for this purpose.
It is located in XLLIB.
Currently it creates the log_idx and log_data files
with read/write acces for everybody.
You can change those permissions but any process that
updates a file that is being logged must have permission
to update the log as well.&lt;/p&gt;
&lt;hr&gt;
&lt;h2&gt;4C Logging - Programs&lt;span class="hx:absolute hx:-mt-20" id="4c-logging---programs"&gt;&lt;/span&gt;
&lt;a href="#4c-logging---programs" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;The lh.fm program is the main log definition program.
You can access this program from the sys.menu.tv program under
Environment/Log Maintenance.
The display fields shown are:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;LogName - unique log name&lt;/li&gt;
&lt;li&gt;Description&lt;/li&gt;
&lt;li&gt;Directory - You must specify the full path of a valid log directory. Use the newlog.sh script to create one if necessary.&lt;/li&gt;
&lt;li&gt;KeepFor - Enter the number of days you want to keep the log data for. This should be less than 31 but for most uses will be much smaller, somewhere between 3 and 7. The log.purge.1 program uses this number to determine which log_idx records to purge and which log_data files to truncate.&lt;/li&gt;
&lt;li&gt;NUpdates - This gets updated everytime a rcd gets written to this log. Set it to 0 anytime you want.&lt;/li&gt;
&lt;li&gt;SaveBeforeImage - Check this if you want the log to maintain a copy of the rcd before it was modified or deleted. Records being added never have a BeforeImage.&lt;/li&gt;
&lt;li&gt;SaveAfterImage - Check this if you want the log to maintain a copy of the changed rcd. Deleted rcds never have an AfterImage.&lt;/li&gt;
&lt;li&gt;LogAdd - Check this if you want the log to log rcd Adds.&lt;/li&gt;
&lt;li&gt;LogMod - Check this if you want the log to log rcd modifications.&lt;/li&gt;
&lt;li&gt;LogDel - Check this if you want the log to log deletes.&lt;/li&gt;
&lt;li&gt;LogFWrite - Check this you want the log to log forced writes. A forced write is an update where 4C cannot tell if the rcd is being added or modified since no read was done of the rcd.&lt;/li&gt;
&lt;li&gt;LogUnhanged - Check this if you want to log updates that result in no change to the rcd.&lt;/li&gt;
&lt;li&gt;Disabled - Check this to disable the log.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Once you&amp;rsquo;ve added a log_hdr record, you can add details.
From the LogDefinition screen, go to the detail screen by
selecting details.
The display fields for the details screen are:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Sequence - sequence number&lt;/li&gt;
&lt;li&gt;Filename - The 4c filename&lt;/li&gt;
&lt;li&gt;ExtName - The external file name&lt;/li&gt;
&lt;li&gt;Dis? - &amp;lsquo;y&amp;rsquo; if this file is disabled from logging.&lt;/li&gt;
&lt;li&gt;DBType - Valid only for external files. This currently can be one of: &lt;code&gt;Oracle Postgres Sybase For an ODBC database, it must be the same as that specified in the sys.ext_type file for that external type.&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;DBName - Valid only for External files.&lt;/li&gt;
&lt;li&gt;DBServer - Valid only for External files. Currently this should only be set for Sysbase Database external files. It should be the same as the sys.et_server field in the sys.ext_type file. If the external database does not use this field, then it should be left blank.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;There are also several cmdbtns at the bottom that allow you
to enable or disable groups of files or to verify
that the Ext Name exists.
Currently VerifyExtnames only works with JISAM files.&lt;/p&gt;
&lt;p&gt;There is a simple log viewer program that you can
run from the Log Maintenance program.
You have a choice of viewing either QuickDetails or
PKeyDetails.
The QuickDetails option does not need to open the log_data
file.
It only needs to read the correct log_idx file.
Hence, it runs faster.
The PKeyDetails shows the same info, plus the primary key
of the record changed.
From either of these detail screens you can view full BeforeImage and
AfterImage detail.&lt;/p&gt;
&lt;p&gt;The le.fm program is used to view any logging errors
You can access this program from the sys.menu.tv program under
Environment/Log Errors.
You should run this program regularly until
you are sure your logging is working properly.&lt;/p&gt;
&lt;hr&gt;
&lt;h2&gt;4C Logging - System PCLs&lt;span class="hx:absolute hx:-mt-20" id="4c-logging---system-pcls"&gt;&lt;/span&gt;
&lt;a href="#4c-logging---system-pcls" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;4C provides several PCLs for reading
a log.
They are:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href="https://127.0.0.1:8080/docs/syspcls/logopen/"&gt;sys.log_open()&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href="https://127.0.0.1:8080/docs/syspcls/logclose/"&gt;sys.log_close()&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href="https://127.0.0.1:8080/docs/syspcls/logseek/"&gt;sys.log_seek()&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href="https://127.0.0.1:8080/docs/syspcls/logread/"&gt;sys.log_read()&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href="https://127.0.0.1:8080/docs/syspcls/loggetattr/"&gt;sys.log_getattr()&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href="https://127.0.0.1:8080/docs/syspcls/loggetname/"&gt;sys.log_getname()&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href="https://127.0.0.1:8080/docs/syspcls/loggetval/"&gt;sys.log_getval()&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href="https://127.0.0.1:8080/docs/syspcls/logcopyflds/"&gt;sys.log_copyflds()&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href="https://127.0.0.1:8080/docs/syspcls/logerror/"&gt;sys.log_error()&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;When the above PCLs return an error, sys.errno
will be set to one of the following:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;1 - LOG_ERRCONN - Connection errro&lt;/li&gt;
&lt;li&gt;2 - LOG_ERROPEN - Open Failed&lt;/li&gt;
&lt;li&gt;3 - LOG_ERRNF - The specified &lt;code&gt;&amp;lt;lclname&amp;gt;&lt;/code&gt; was not found as a currently open log.&lt;/li&gt;
&lt;li&gt;4 - LOG_ERREOF - End of file while reading log&lt;/li&gt;
&lt;li&gt;5 - LOG_ERRNORCD - There is no current log rcd.&lt;/li&gt;
&lt;li&gt;6 - LOG_ERRNOFILEDEF - No file definition. This is an internal error that you should never see.&lt;/li&gt;
&lt;li&gt;7 - LOG_ERRARG - Invalid or missing argument.&lt;/li&gt;
&lt;li&gt;8 - LOG_ERRNOIMAGE - No rcd image exists of the type needed.&lt;/li&gt;
&lt;li&gt;999 - Unknown Error&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Using the above PCLs it is fairly easy
to implement a Data Replication Application
between different 4C Servers.&lt;/p&gt;
&lt;hr&gt;
&lt;h2&gt;4C Logging - Misc&lt;span class="hx:absolute hx:-mt-20" id="4c-logging---misc"&gt;&lt;/span&gt;
&lt;a href="#4c-logging---misc" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;ul&gt;
&lt;li&gt;Because the log_data and log_idx files can get large, you should make sure you keep logs only as long as necessary and run the log.purge.1 program regularly.&lt;/li&gt;
&lt;li&gt;If you are using the log.purge.run program to purge log data, you must verify the doit() PCL. It used to not loop, then at one time it looped continuously without sleeping. The PCL should look like this: &lt;code&gt;while(1) { if (curdate != sys.cur_date) { curdate = sys.cur_date; sys.push_prog(&amp;quot;log.purge.1&amp;quot;); } sleep(3600); }&lt;/code&gt; This has been fixed in 4C Server Version 4.4.6-3 and higher&lt;/li&gt;
&lt;li&gt;Multiple log definitions are allowed&lt;/li&gt;
&lt;li&gt;One file may be logged by more than one log.&lt;/li&gt;
&lt;li&gt;Even though it is possible to log the same 4cfile without different external names in the same log, it is not a good idea. When reading the log it will be impossible to tell which of the external files was being updated. Use different logs for this purpose.&lt;/li&gt;
&lt;li&gt;4C will notice when file definitions change and the data written to the log will always reflect the correct file definition.&lt;/li&gt;
&lt;li&gt;Don&amp;rsquo;t try to log any of the following files: &lt;code&gt;log_idx log_fdhdr log_fddet&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;It is possible to log database files as well as 4C files, but it is not possible for 4C to know when non 4c programs make changes to database files. Neither is it possible for 4C to know what changes were made by an SQLStatement in a 4C program. If you need to log database files, you really should be using the DBMS tools for this.&lt;/li&gt;
&lt;li&gt;If more than one instance of 4c shared mem is running on a system and the different instances log the same data files, then the log_hdr, log_det, log_err log_fdhdr, and log_fddet files must be the same. The best way to accomplish this is to make symbolic links for these files between the different system directories.&lt;/li&gt;
&lt;li&gt;Do not define more than one log using the same directory Currently there is no check for this but it is guaranteed not to work.&lt;/li&gt;
&lt;li&gt;After defining logs, you should monitor them for size and errors.&lt;/li&gt;
&lt;li&gt;The system log info is initialized only when the 4c system memory is first allocated, so changes made to a log definition have no affect until all 4c process have been stopped. It is not necessary to stop the 4csrvrd process.&lt;/li&gt;
&lt;li&gt;It is very important to make sure your filesystem has enough space for your log files.&lt;/li&gt;
&lt;li&gt;Even when BeforeImage is selected, it is not always possible to know the BeforeImage rcd. The most common reasons for this are: The application uses F_NODEBLOCK when reading a rcd The Application calls sys.upd_file() without previously calling sys.read_file(). An external program, like the 4c utilities, use FCupdfile() without calling FCreadfile()&lt;/li&gt;
&lt;li&gt;Currently there is no program that can be used for crash recovery.&lt;/li&gt;
&lt;/ul&gt;</description></item><item><title>4C Program Logging - Overview</title><link>https://127.0.0.1:8080/docs/features/logging/prlogov/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://127.0.0.1:8080/docs/features/logging/prlogov/</guid><description>
&lt;p&gt;&lt;a href="https://127.0.0.1:8080/docs/features/logging/"&gt;4C Logging Home&lt;/a&gt;
The program logging facility allows you to keep a log of who ran specific programs and the values
of application specific file variables at specific program states.
This makes it easy to meet auditing requirements without needing to change any program.
The states of a program where a log record can be generated are&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Init&lt;/li&gt;
&lt;li&gt;Open&lt;/li&gt;
&lt;li&gt;Start Field Loop&lt;/li&gt;
&lt;li&gt;End Field Loop&lt;/li&gt;
&lt;li&gt;End&lt;/li&gt;
&lt;li&gt;Exit&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Any single program can generate log records for 1 or more program states.
The Start Field Loop and End Field Loop states may generate multiple log rcds during one
instance of running a program.
If you need to log the value of sys.mode,
then you will need to make sure a log rcd is written during the End Field Loop state since
that is the only state where it may be set to somthing other than &amp;ldquo;Lookup&amp;rdquo;.&lt;/p&gt;
&lt;p&gt;Program logging is application specific not global.
The files used for program logging are stored in the Application Program directory along with
other system files.
The logging files are&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;log_proghdr - The main hdr file for defining the program log. Used to specify a name for the log, description, whether it is disabled, how many days is the log maintained before it is purged, and to store some program log statistics.&lt;/li&gt;
&lt;li&gt;log_progdet - This specifies each program that is part of this specific log, which states to write log rcds and the names of the file variables to write to the program log.&lt;/li&gt;
&lt;li&gt;log_progdata - This is the actual log and is written automatically by the system.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;An application that uses program logging must purge the log by calling the lprog.purge.1 program
sometimes,
presumably from one of the nightly or weekly maintenance scripts used by the application.&lt;/p&gt;
&lt;p&gt;It is also assumed that if the application wants to maintain a permanent log,
that it will read the log_progdata file and update the application permanent log.
This can be done from a remote system or on the same system where the application is running.&lt;/p&gt;
&lt;p&gt;In order to create or modify a program log,
select the &amp;ldquo;Program Logging Definitions&amp;rdquo; under the &amp;ldquo;Logging&amp;rdquo; submenu of the main system treeview
menu.
Or, you can just run lprogh.fm.&lt;/p&gt;
&lt;p&gt;Program logging changes take effect only when 4C Shared memory is initialized,
that is each time all 4C processes have ended and a new one is started.
It is not necessary to restart the 4csrvrd process as it does not use 4C shared mem.&lt;/p&gt;</description></item></channel></rss>