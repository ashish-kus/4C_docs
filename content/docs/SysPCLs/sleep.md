---
title: "sleep()"
description: "pauses for <count> seconds"
weight: 319
draft: false
---

## Purpose

`sleep()` pauses for `<count>` seconds.

## Usage

```text
sleep(<count>);
```

## Arguments

- `integer <count>` — The number of seconds to sleep for.

## Returns

sleep always returns 0.

## Where Used

`sleep()` can be called from anywhere.

## Example

```text
sleep(3);
```

## Description

`sleep()` pauses for `<count>` seconds.

## See Also

Back to Top

