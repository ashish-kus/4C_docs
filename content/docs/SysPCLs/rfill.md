---
title: "sys.r_fill()"
description: "will right pad <originalstring> with <fillchar> up to length <maxlen"
weight: 259
draft: false
---

## Purpose

`sys.r_fill()` will right pad `<originalstring>` with `<fillchar>` up to length `<maxlen>`

## Usage

```text
<aret> = sys.r_fill(<originalstring>,<fillchar>,<maxlen>);
```

## Arguments

- `alpha <originalstring>` — The alpha variable to right fill.

## Arguments

- `alpha <fillchar>` — An alpha variable of length 1 to use as the fill char.

## Arguments

- `integer <maxlen>` — The maximum length to fill `<originalstring>` to.

## Returns

- `alpha <aret>` — A copy of `<originalstring>` right filled with `<fillchar>` to a length
of `<maxlen>`

## Where Used

`sys.r_fill()` can be called from anywhere.

## Description

Requirements

## See Also

Sys PCLs List

Back to Top

## See Also

- [Sys PCLs List]({{< ref "_index.md" >}})

